<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= title || 'Bertie’s Books' %></title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <header class="site-header">
    <nav class="nav">
      <div class="brand"><%= shopName || "Bertie’s Books" %></div>
      <a href="/" class="<%= (active==='home')?'active':'' %>">Home</a>
      <a href="/books" class="<%= (active==='list')?'active':'' %>">Books</a>
      <a href="/books/add" class="<%= (active==='add')?'active':'' %>">Add book</a>
      <a href="/search" class="<%= (active==='search')?'active':'' %>">Search</a>
      <div style="margin-left:auto"></div>
    </nav>
  </header>
  <main class="container">
